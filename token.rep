
const void *p_element;

enum codi _s; //codi del token

//Funcions privades
void print_tp() const throw(error);
/*Aquesta funció treu per pantalla de quin tipus és aquest token (NULLTOK, CT_ENTERA, etc.)
depenent del seu codi */

bool valid_var(const string & var);
/*Aquesta funció ens indica si el nom que ens estan proposant per a anomenar una variable és 
correcte*/